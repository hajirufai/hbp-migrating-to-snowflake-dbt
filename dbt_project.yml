name: hbp_learning_analytics
version: 1.0.0
config-version: 2

profile: hbp_learning_analytics

model-paths: ["models"]
analysis-paths: ["analyses"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  hbp_learning_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

seeds:
  hbp_learning_analytics:
    +schema: raw
    +quote_columns: false
